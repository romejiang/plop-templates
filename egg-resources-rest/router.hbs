// insert
  // ===========================================================
  // ======================  {{ name }}  =======================
  // ===========================================================
  router.post('/api/{{ name }}/pass/:id', controller.{{ name }}.pass)    //  开启，关闭
  router.get('/api/{{ name }}/mine', controller.{{ name }}.mine)     //  根据个人获取信息
  router.resources('{{ name }}', '/api/{{ name }}', controller.{{ name }})
  // ===========================================================